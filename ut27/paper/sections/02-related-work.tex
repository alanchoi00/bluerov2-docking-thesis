\section{Related Work}
\label{sec:related}

\textbf{Docking with moving stations.} Docking guidance is staged by sensing modality, with acoustic or electromagnetic homing bringing the vehicle within camera range and vision closing the final metres \cite{yazdani2020survey,maire2009vision}. In nearly all of it the dock is a fixed pose in the world frame and environmental disturbance is a steady current on the vehicle. The exceptions are partial. Trslic et al.\ docked a work-class ROV to a tether management system heaving 1.1~m peak to peak at 8.5~s by servoing on the latest camera pose, holding the last pose through dropouts and setting the depth setpoint to the cage's mean depth; they report that the vehicle's inertia made compensation infeasible and contact inevitable \cite{trslic2020vision}. Their neuro-fuzzy predictor forecast the cage depth up to 2.5~s ahead within the cage's 0.55~m tolerance, but from a pressure sensor on the cage, for heave only, offline \cite{trslic2020neuro}. Suspended and moored docks move too: a seabed-moored suspended dock under steady current swings by more than a metre and oscillates in yaw without settling, and its designers address this with structure and passive funnel contact rather than estimation \cite{guo2024dynamic}. A resident-vehicle dock at shallow depth was found to shed the vehicle under wave forcing several metres below the surface, which is why its deep site was chosen \cite{gunzel2026docking}. A docking-station ROV that captures an AUV in mid-water makes both bodies free \cite{morinaga2021development}. None of these estimates the dock's wave-frequency motion through the vehicle's camera or feeds it forward.

\textbf{Estimating a moving target from a moving observer.} Target tracking supplies the kinematic hierarchy: a constant-position model absorbs target motion as process noise, a constant-velocity model adds a velocity state driven by white acceleration, and the acceleration density sets the trade between responsiveness and noise amplification \cite{barshalom2001estimation,li2003survey}. Every member inherits the assumption that measurement errors are white and independent of the state; sequentially correlated measurement noise requires state augmentation or a differenced measurement, and no process-noise setting substitutes for it \cite{bryson1968correlated}. When the target is measured relative to the observer, the natural formulation is a relative state with the observer's own velocity as a known input, as in spacecraft relative navigation \cite{kim2007relative,woffinden2007angles}; the observer's velocity error then enters the process model instead of the measurement. Wave-driven platform motion has been estimated as a second-order oscillator in a Kalman filter for ship motion \cite{triantafyllou1983ship} and modelled as an internal model for landing on an oscillating deck \cite{marconi2002landing}; the constant-velocity model used here is deliberately model-free about the motion, because a real dock's motion is not a single sinusoid. In the docking literature the filter is a noise suppressor for a static dock \cite{gunzel2026docking,ni2025vision}, or a disturbance observer on the vehicle with a constant-input random-walk state \cite{shi2023integrated}; the case of a velocity state on the target, fed forward, is not examined.

\textbf{Visual servoing and latency.} Position-based visual servoing regulates a reconstructed relative pose in Cartesian space, which costs nothing extra when the marker pipeline already produces a full pose \cite{chaumette2006visual,garrido2014automatic}. Tracking a moving target adds a feedforward of the estimated target velocity to the servo law \cite{chaumette2007visual}, and the closed-loop behaviour then depends on the delays and dynamics between the command and the vehicle's response, which in a visual loop are routinely large enough to matter \cite{corke1996dynamic}. On the vehicle side the command channel of the production autopilot is effort-like rather than a velocity setpoint, so the feedforward gain must be matched to an identified plant gain, a point that the failure analysis of Section~\ref{sec:failure} turns on.
