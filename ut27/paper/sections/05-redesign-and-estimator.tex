% The T5 decision (issue UNSWROV/bluerov2-docking#67) is pending: either the
% control-first redesign (calibrated or velocity-closed feedforward plus a
% velocity-state decay while stale) or the relative-frame estimator promised
% in the submitted abstract. The section title and placeholder below fit
% either outcome; rewrite once the decision is made.
\section{Feedforward Redesign and Estimator}
\label{sec:redesign}

Placeholder, budget 0.75 page. The changes evaluated as arm C, with equations and a signal-path diagram against the original arm B, and a statement of what is unchanged (fusion, gating, bounds, controllers, state machine). If the relative-frame estimator is included, its state, process input and measurement model go here with the covariance propagation of the vehicle-velocity input.
