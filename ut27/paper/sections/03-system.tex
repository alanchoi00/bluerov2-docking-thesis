\section{System}
\label{sec:system}

\subsection{Simulation and dock motion}

The vehicle is a BlueROV2 Heavy in Gazebo (gz-sim~8, DART physics) with the production ArduSub firmware executed software-in-the-loop through the ArduPilot Gazebo plugin, so the path from velocity command through mode controller to motor mixing is the code a physical vehicle runs. The vehicle model, hydrodynamics and bring-up derive from the open-source Blue framework \cite{blue2024software,palmer2024angler}, with the drag coefficients aligned to published BlueROV2 Heavy identification. The forward camera runs at 10~Hz. The world places the docking station 5~m ahead of the spawn point at 1.5~m depth. A plugin drives the dock through a prescribed sinusoidal sway and heave of 0.1~m amplitude with a 90$^\circ$ relative phase, an orbital motion representative of wave forcing on a moored structure, with period and start phase set per trial. Mechanical capture is not modelled; success is defined at the capture envelope and contact with the structure is detected in scoring. The vehicle pose used by every node is the Gazebo ground-truth odometry at 100~Hz, relayed into the transform tree; Section~\ref{sec:failure} depends on this, and Section~\ref{sec:design} replaces it with a corrupted navigation solution.

\begin{figure*}[t]
  \centering
  \input{figures/tikz/architecture}
  \caption{System architecture. The state machine gates the controllers on the filter's health signal; the dashed vehicle-to-camera path is the coupling examined in Section~\ref{sec:failure}.}
  \label{fig:arch}
\end{figure*}

\begin{figure*}[t]
  \centering
  \input{figures/tikz/filter-structure}
  \caption{Filter structure: the nine-state error-state filter with the two process-noise regimes, the measurement gate and the state bounds.}
  \label{fig:kf}
\end{figure*}

\subsection{Perception}

The dock face carries nine ArUco markers \cite{garrido2014automatic} in three size classes from 200~mm to 47.5~mm, placed so that the large outer pair carries acquisition at range and the small pair at camera eye level stays in frame during the final centimetres, in the spirit of fractal markers distributed spatially rather than nested \cite{romeroramirez2019fractal}. Each detected marker implies a dock origin through the surveyed layout. A spatial-consensus test on the implied orientations rejects outliers \cite{kim2024enhancing}, and the survivors are fused by inverse-variance weighting \cite{xu2021underwater} with a range-dependent noise model, $\sigma_p = \alpha r^2/s$ for a marker of side $s$ at range $r$, so that distant and small markers count less. The fused pose, in the camera frame, is transformed to the world frame through the vehicle pose at the image stamp and fed to a nine-state error-state Kalman filter over dock position, dock velocity and a small-angle attitude correction \cite{sola2017quaternion} (Fig.~\ref{fig:kf}). Two process-noise regimes define the two estimators compared throughout: \emph{static}, with zero velocity uncertainty and zero velocity process noise, which is a constant-position filter; and \emph{sway}, with white-acceleration noise of density $\sigma_a$ (0.16~m/s$^2$ nominal), which estimates dock velocity. Each update passes a Mahalanobis gate, the velocity state is bounded at 0.2~m/s, and a health signal reports \textsc{healthy}, \textsc{degraded} or \textsc{stale} from the update age and the position covariance.

\subsection{Guidance, control and supervision}

Guidance converts the filtered dock pose and the vehicle pose into a body-frame error to a target on the dock entry axis, and each phase controller regulates it with position-based visual servoing \cite{chaumette2006visual} (Fig.~\ref{fig:arch}). The coarse controller drives to a standoff point 1~m out along the entry axis with per-axis proportional-derivative gains and a distance-gated surge cap, in the autopilot's depth-hold mode; the handoff to fine alignment is gated on position, boresight offset, yaw and a velocity-match test, debounced so that a sway zero crossing cannot latch it. The fine controller runs align-then-advance in stabilise mode, holding lateral, vertical and yaw tolerances before each step forward, and declares the vehicle seated at a debounced 0.10~m from the entry with 0.03~m lateral and vertical tolerance. A supervisory state machine sequences \textsc{coarse}, \textsc{fine} and \textsc{docked}, demotes on loss of perception, and gates every controller on the filter's health, so a \textsc{stale} estimate produces zero command.

Both controllers accept a dock-velocity feedforward, built from the filter's velocity state rotated into the body frame, clamped at 0.25~m/s and added to the feedback command. The command channel is effort-like: the autopilot maps it to thrust and the vehicle velocity follows with a gain and a lag. The coarse controller scales the feedforward by a gain of 0.6, ramped in with distance to the standoff, to convert a physical velocity into command units on the basis of a plant gain of 1.3 to 1.6 and a lag of about 240~ms read from a single early trial; the fine controller, which operates at close range, passes it at unit gain, halved while the filter is degraded. In the static regime the velocity state is zero, so the baseline carries no feedforward and the two estimators differ only in the velocity state.
