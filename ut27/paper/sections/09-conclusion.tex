\section{Conclusion}
\label{sec:conclusion}

Adding a dock-velocity feedforward to a static-dock visual docking pipeline turned a reactive baseline that docks at every sway period into a method that fails at the wave periods of its operating site, and its own estimator pointed at itself. The recorded trials showed the estimator was right: the measurement was clean, the velocity state tracked the dock, and the vehicle over-swung because the feedforward was scaled for a plant gain of 1.3 to 1.6 where the command channel delivers 2.7 with a 0.4~s time constant. An oracle feedforward from the true dock velocity over-swings identically, so the limit was the open-loop law, not the estimate. Closing the feedforward as a body-velocity setpoint on the vehicle's own navigation velocity removes the over-swing at every period, docks cleanly from 20~s to 8~s with entry-plane offsets under 2~cm, and cancels a slow navigation-velocity bias that no camera-based estimator, in the world frame or the relative frame, can observe. Bounding the velocity state during marker blackout removes the metre-scale dead-reckoning that produced the apparent phantom velocity. The remaining limit at 6~s is the handoff gate, not tracking. For a deployment the order of importance is the velocity loop, then the bounded velocity state, then the choice of estimator; and every diagnostic the system exposed pointed the wrong way, so a feedforward on any moving target needs an independent measurement of the vehicle's actual response before its estimator is blamed.
